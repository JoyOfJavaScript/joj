{
  "name": "joj-blockchain",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-core": "^6.26.0",
    "babel-eslint": "^8.0.2",
    "babel-plugin-transform-builtin-extend": "^1.1.2",
    "babel-plugin-transform-decorators": "^6.24.1",
    "babel-plugin-transform-flow-strip-types": "^6.22.0",
    "babel-plugin-transform-regenerator": "^6.26.0",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-es2015": "^6.22.0",
    "babel-preset-flow": "^6.23.0",
    "babel-preset-stage-3": "^6.24.1",
    "chai": "4.1.0",
    "eslint": "^4.18.0",
    "eslint-plugin-flowtype-errors": "^3.3.6",
    "eslint-plugin-fp": "^2.3.0",
    "flow-bin": "^0.59.0",
    "mocha": "^5.0.5",
    "nodemon": "^1.11.0",
    "sinon": "^1.17.7",
    "sinon-chai": "^2.8.0",
    "standard": "^11.0.1",
    "transform-runtime": "0.0.0"
  },
  "dependencies": {
    "joj-adt": "file:../adt"
  },
  "scripts": {
    "lint": "eslint src/",
    "lint-fix": "eslint src/ --fix",
    "start": "babel-node src/main.js",
    "test": "mocha --recursive --require babel-register --require babel-polyfill --check-leaks test/",
    "test-dev": "nodemon --watch src/ --watch test/ --exec 'mocha --recursive --require babel-register --require babel-polyfill test/ || true'",
    "test-debug": "mocha --source-maps --inspect-brk --watch --recursive --require babel-register --require babel-polyfill --no-timeouts",
    "flow": "flow",
    "flow-init": "flow init",
    "flow-status": "flow status"
  }
}
